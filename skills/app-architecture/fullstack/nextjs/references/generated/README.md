# Generated Next.js References

Auto-populated by `/composure:initialize` via Context7 queries.

These docs contain up-to-date API patterns for Next.js (App Router, Server Components, Server Actions, proxy.ts). Populated when `frontend: "nextjs"` is detected.

## Refresh

```bash
/composure:initialize --force
```
