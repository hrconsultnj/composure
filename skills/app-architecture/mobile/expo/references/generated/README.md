# Generated Expo References

Auto-populated by `/composure:initialize` via Context7 queries.

These docs contain up-to-date API patterns for Expo (expo-router, React Native, EAS build). Populated when `frontend: "expo"` is detected.

## Refresh

```bash
/composure:initialize --force
```
