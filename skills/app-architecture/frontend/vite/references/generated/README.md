# Generated Vite References

Auto-populated by `/composure:initialize` via Context7 queries.

These docs contain up-to-date API patterns for Vite (Environment API, SPA config, dev server, proxy, HMR). Populated when `frontend: "vite"` is detected.

## Refresh

```bash
/composure:initialize --force
```
