# Generated Framework References

Auto-populated by `/composure:initialize` via Context7 queries.

These docs contain up-to-date API patterns, anti-patterns, and configuration for the detected framework versions. They supplement the hand-curated docs in `../typescript/`.

Shared TypeScript/React patterns (typescript, shadcn, tailwind) live here. Framework-specific generated docs live in their respective folders (e.g., `frontend/vite/references/generated/`, `fullstack/nextjs/references/generated/`).

## Refresh

```bash
/composure:initialize --force
```

## Contributing

If a generated pattern is stable and broadly useful, move it to `../universal/` and submit a PR to the plugin.
