# Generated Angular References

Auto-populated by `/composure:initialize` via Context7 queries.

These docs contain up-to-date API patterns for Angular (standalone components, signals, functional guards, zoneless change detection). Populated when `frontend: "angular"` is detected.

## Refresh

```bash
/composure:initialize --force
```
