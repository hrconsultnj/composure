{
  "name": "composure-guardrails",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AIGRaaS evaluation engine — MCP STDIO + CLI transports",
  "scripts": {
    "prebuild": "bash scripts/copy-engine.sh",
    "build": "tsc && node esbuild.config.js",
    "build:check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
